# Dashboard Construction Task

- `[x]` Reorganize routing into `(main)` and `(dashboard)` Route Groups to isolate the global `Navbar` and `Footer`.
- `[x]` Update `src/app/layout.tsx` to only act as the root provider shell.
- `[x]` Create `(main)/layout.tsx` with `Navbar` and `Footer`.
- `[x]` Create `(dashboard)/layout.tsx` with a premium Side Navigation and Top Header containing User details and alerts.
- `[x]` Create `src/app/(dashboard)/dashboard/page.tsx` (the Dashboard Overview).
- `[x]` Build the IoT Telemetry UI components (NPK levels, Electrical Conductivity, Soil Moisture, Temp, Humidity, Wind).
- `[x]` Integrate functional widgets (Mandi prices, Recent Activity, Weather).
- `[x]` Ensure premium, cinematic, glassmorphic UI matching `DashboardPreview`.
- `[/]` Verify all routes and links are working correctly.
